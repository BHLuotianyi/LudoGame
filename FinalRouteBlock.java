/**
 * Block in a player's final route toward the finish.
 */
public class FinalRouteBlock extends MapBlock {
    /**
     * Creates a final-route block.
     *
     * @param index the block's index in the game map
     * @param color the owner color of this final-route block
     */
    public FinalRouteBlock(int index, String color) {
        super(index, color);
        //d 
    }
}
